import { Metadata } from "next";

export const metadata: Metadata = {
  title: "pricing",
  description: "pagina de precio",
  keywords: ['SEO', 'Title','pricing'],    
};

const pricingPage = () => {
  return <div>pricing Page</div>;
};

export default pricingPage;
